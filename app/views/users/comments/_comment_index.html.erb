
		<% exhibition.comments.each do |comment| %>
		<%= comment.comment %>
		<% if comment.user_id == current_user.id %>
		<%= link_to "×", users_exhibition_comments_path(exhibition_id: exhibition, id: comment.id), method: :delete ,remote: true %>

		<% end %><br>
		<% end %>










